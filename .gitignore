# All data dirs are reproducible artifacts — recreate via `scripts/build_all.py`.
# Snapshots ship as GitHub Release assets, not via git.
data/raw/
data/interim/
data/processed/

# Per-country web JSON: rebuilt by CI from data/processed/.
# (countries.json + colors.json are tiny and committed for fast first paint.)
web/polls_*.json
web/version.json

# Build / cache / venv artifacts
__pycache__/
*.pyc
*.tmp.*
.venv/
*.egg-info/
build/
dist/

# Editor
.vscode/
.idea/
*.swp

# Session logs (per-machine notes)
session_logs/

# Misc
.DS_Store
